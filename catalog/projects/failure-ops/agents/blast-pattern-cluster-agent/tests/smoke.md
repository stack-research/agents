# Smoke: blast-pattern-cluster-agent

- Run example input; expect at least one cluster and valid `blast_pattern` values.
- Empty `failure_modes` must error.
- Missing `failure_mode_id` in input should error.
