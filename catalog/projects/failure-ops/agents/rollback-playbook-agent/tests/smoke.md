# Smoke: rollback-playbook-agent

- Run example input; expect non-empty `steps`, `verification_checks`, and `abort_conditions`.
- Empty `clusters` must error.
- Missing required cluster fields should error.
