# Smoke: failure-library-agent

- Run example input; expect non-empty `failure_modes` and `library_status` in `complete|partial`.
- Empty `observations` must error.
- Non-object observation entries must error.
