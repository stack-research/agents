# Smoke test

- Run deterministic mode with `examples/example-input.json`.
- Verify at least one correlated event for same-service change timelines.
- Verify confidence stays in `[0,1]` and summary is non-empty.
