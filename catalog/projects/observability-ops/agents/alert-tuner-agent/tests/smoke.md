# Smoke test

- Run deterministic mode with `examples/example-input.json`.
- Verify recommendations are bounded and include threshold deltas.
- Verify noise_score stays in `[0,1]` with non-empty rationale.
