# Smoke checks

- Baseline and current with one metric drop beyond threshold yields `regression_flag` true and `verdict` warn or fail.
- Identical maps yield `regression_flag` false and `verdict` pass.
