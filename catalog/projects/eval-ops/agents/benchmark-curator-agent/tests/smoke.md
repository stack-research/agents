# Smoke checks

- Example input produces a `curated_suite_id`, non-empty `included_cases`, and a valid `curation_verdict`.
- Duplicate `case_id` values appear in `excluded_duplicates` and not twice in `included_cases`.
