# Smoke Test

Input:

- `examples/example-input.json`

Expected:

- Output includes `active_count`, `critical_items`, `handoff_brief`, and `recommended_checks`.
- `critical_items` has 0 to 3 entries.
- `recommended_checks` contains exactly 3 entries.
