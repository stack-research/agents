# Smoke Checks

## Case 1: access issue

Input category `access`, priority `p2` -> subject references access/sign-in and includes timeline.

## Case 2: billing issue

Input category `billing`, priority `p3` -> reply references billing queue and follow-up window.

## Case 3: high severity

Input priority `p1` -> reply communicates urgent handling and near-term update.

## Case 4: missing optional name

No `customer_name` -> greeting should remain polite without name placeholder artifacts.
