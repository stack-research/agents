# Smoke Test

Input:

- `examples/example-input.json`

Expected:

- Output includes `verdict`, `findings`, and `risk_level`.
- `verdict` is one of `pass`, `fail`, `review`.
- `findings` has 1 to 5 entries.
- `risk_level` is one of `low`, `medium`, `high`.
